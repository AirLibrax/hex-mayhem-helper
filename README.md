# 海克斯大乱斗助手（Hex Mayhem Helper）

LOL 极地大乱斗（ARAM）辅助悬浮窗：选人/载入阶段显示英雄胜率，对局中自动识别海克斯符文弹窗并给出**当前英雄的符文适配胜率**、装备推荐与**海克斯组合提示**。

> 本项目与 Riot Games 无关，非官方出品。Riot Games 及其相关商标归 Riot Games, Inc. 所有。

## 展示结构（按游戏阶段）

悬浮窗为无边框、置顶、半透明、可拖动的深色面板，按 LCU 对局阶段动态切换内容区。以下为各阶段的完整展示结构。

### 1. 默认 / 主界面（未连接客户端）

```
海克斯大乱斗助手    未连接客户端，请先启动游戏客户端        ⚙ ×
（标题行：标题 + 状态 + 透明度/设置/关闭按钮）

（内容区为空——阵容/符文/装备全部隐藏）

v0.5.7                -From AirLibrax ◢
（版本号·左下角      水印+resize三角·右下角）
```

- 客户端连接成功 → 状态变为「已连接客户端」，随后跟随对局阶段流转

### 2. 选人阶段（ChampSelect）

```
海克斯大乱斗助手    ChampSelect    ⚙ ×

我方队伍平均胜率：53.7%
  [T3] 茂凯 胜率:49.7% 选取率:8.5%
  [T1] 薇恩 胜率:57.7% 选取率:13.2%
  [T2] 安妮 胜率:...
  ...（最多 5 行，玩家已选英雄，含自己）

未被选择的英雄
  [T5] 奥拉夫 胜率:44.9% 选取率:1.5%
  [T4] 加里奥 胜率:49.5% 选取率:7.1%
  ...（公共台 bench 英雄，换人/重随自动刷新）

v0.5.7                -From AirLibrax ◢
```

- 仅显示**我方**（敌方不在选人阶段显示）
- 公共台英雄独立分区块，全部选完（公共台为空）自动隐藏
- 玩家换人/重随 → 阵容自动刷新

### 3. 载入画面（GameStart）

```
海克斯大乱斗助手    GameStart    ⚙ ×

我方队伍平均胜率：53.7%
  [T3] 茂凯 胜率:49.7% 选取率:8.5%
  [T1] 薇恩 胜率:57.7% 选取率:13.2%
  ...

敌方队伍平均胜率：47.9%
  [T2] 亚索 胜率:...
  [T4] 劫 胜率:...
  ...

v0.5.7                -From AirLibrax ◢
```

- 显示**我方 + 敌方**全部 10 名英雄胜率
- 进入对局后阵容区收起

### 4. 对局中（InProgress）

```
海克斯大乱斗助手    InProgress    ⚙ ×

装备推荐（常驻）
  [T1] 主流 · 胜率 53.1% · 样本 14804
  核心: 狂战士胫甲 → 破败王者之刃 → 鬼索的狂暴之刃
  出门: 多兰之刃 + 生命药水 · 召唤师: 闪现/幽灵疾步

符文推荐（仅检测到海克斯弹窗时出现）
  [T1] 坦克引擎 · 胜率51.3% · 选取率28.9% · 组合:钢化你心+升级：献祭(63.0%)
  [T3] 歌利亚巨人 · 胜率50.1% · 选取率14.9%
  [T2] 巨像的勇气 · 胜率:... · 选取率:...

v0.5.7                -From AirLibrax ◢
```

- **装备推荐**：常驻显示（进入对局后不再消失），流派行带 T 级
- **符文区**：仅在检测到海克斯三选一弹窗时出现，选完弹窗消失后自动隐藏
- 符文行尾的 `组合:...(...%)` 为**组合提示**：该符文命中当前英雄 Top5 三符文组合时显示（其他两个成员 + 组合胜率），未命中不显示

### 5. 设置对话框（点 ⚙ 打开）

```
设置                                          ×

胜率数据源
  (•) aramgg.com（官方 API）  ( ) hexdata.com.cn

aramgg API Key [______________] [获取 Key]
  （留空使用内置 Key / api_key.txt 中的 Key）

立即刷新数据    上次更新: 08-19 18:30

符文检测屏幕（多屏时指定游戏所在屏）
  [自动（全部屏幕） ▼]
  手动截图与自动检测立即生效；对局中切换需等下一轮轮询

调试
  [手动识别英雄] [手动识别符文]
  状态反馈（绿色小字）：识别英雄中… / 监听弹窗中… 剩余 N 秒 / 英雄已识别: XX

  -From AirLibrax · v0.5.7
```

- 无边框、置顶、可拖动（与悬浮窗同构）
- 数据源切换 / Key 变更自动触发重新拉取
- 调试通道：手动识别英雄（LCU 读当前英雄 + 拉详情写缓存）→ 手动识别符文（弹窗 OCR，验证英雄适配通路）

### 6. 手动识别结果对话框

```
手动截图识别结果 v0.5.7
检测屏: 屏幕 1
[截图缩略图]
检测到 3 张符文卡（英雄视角数据）：
  · 坦克引擎 · 胜率51.3% · 选取率28.9%
  · 钢化你心 · 胜率52.8% · 选取率17.1%
截图已保存: debug_capture.png (程序目录)
[Close]
```

- 非模态（不阻塞其他窗口），检测屏标注 + 截图缩略图 + 识别明细

## T 级与颜色体系

统一按**胜率分档**（符文 / 装备 / 英雄 / 组合通用）：

| T 级 | 胜率区间 | 颜色 |
|---|---|---|
| T1 | ≥ 50% | 红 `#e05a4e` |
| T2 | ≥ 45% | 金 `#e8b64c` |
| T3 | ≥ 40% | 蓝 `#6cb2ff` |
| T4 | < 40% | 绿 `#6fce8a` |
| T5 | （英雄榜数据源自带） | 白 |

行格式统一：`[T几] 名字 胜率:X% 选取率:X%`（所有行统一字重，仅靠颜色区分）。

## 符文识别与组合提示

- **弹窗检测**：边缘检测（弹窗卡片为深色玻璃底 + 亮色边框竖线）+ 暗化遮罩判定 + 三卡等宽/间距一致性校验（防商店/选人界面误检）
- **符文识别**：Windows 自带 OCR 读卡片标题 → 名称匹配符文库（标点容错 + 编辑距离兜底）
- **英雄适配**：LCU 只读接口识别当前英雄（puuid/summonerId 双标识兜底）→ 详情缓存交叉 → 胜率/选取率优先显示**英雄视角数据**
- **组合提示**：识别符文命中英雄 Top5 组合（aramgg augmentTrios）时，行尾显示相关组合

## 数据源与 API Key

- **主源**：aramgg.com 官方 API（data.dtodo.cn），免费 200 credits/天，本地缓存大幅节省
- **备用**：hexdata.com.cn（设置内切换）
- **Key 优先级**：`api_key.txt`（程序目录）> 设置内填写 > 内置共享 Key > 环境变量 `HEX_MAYHEM_API_KEY`
- 分发包自带 `api_key.txt` 模板：用户填自己的 Key 重启即生效（申请地址 data.dtodo.cn/developer.html，GitHub 授权登录）

## 数据目录（绿色便携）

配置/缓存/日志跟随**程序目录**（整个文件夹可拷走）：`settings.json`、`stats_cache.db`、`app.log`、`api_key.txt`、`debug_capture.png`。目录不可写时回退用户目录 `~/.hex-mayhem-helper/`。

## 运行环境

- Windows 10 / 11（64 位）——OCR 依赖系统自带引擎
- Python 3.11+（源码运行）

## 快速开始（源码）

```bash
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
.venv\Scripts\python main.py
```

## 打包分发（免安装）

```bash
.venv\Scripts\pip install -r requirements-build.txt
python build.py
```

产物：`dist/HexMayhemHelper_v<版本>.zip`——解压后双击 `HexMayhemHelper.exe` 即用，无需安装任何依赖。

## 合规说明

- 数据获取仅走 **LCU 只读接口**（lockfile 认证的本地 API）+ **系统级屏幕截图**
- 不使用游戏内存、无注入、无模拟输入、无流量劫持
- 展示的信息均为玩家客户端内可见内容与公开统计数据
- 依据 Riot 第三方应用政策：面向玩家的产品应在 [Riot Developer Portal](https://developer.riotgames.com/) 注册，使用 LCU API 的产品发布前需联系 Riot 告知（本项目已注册，App ID 872348，状态 Approved）

## 目录结构

```
├── main.py                 # 程序入口
├── src/
│   ├── config.py           # 配置（窗口/数据源/检测屏/Key文件）
│   ├── secrets.py          # API Key（本地，不提交）
│   ├── secrets.example.py  # API Key 配置示例
│   ├── version.py          # 版本号
│   ├── paths.py            # 资源路径（源码/打包兼容）
│   ├── capture/            # 截图 + 弹窗检测 + OCR
│   ├── data/               # aramgg/hexdata 数据源 + SQLite 缓存
│   ├── lcu/                # LCU 只读接口
│   └── ui/                 # 悬浮窗 + 设置对话框
├── requirements.txt        # 运行时依赖
├── requirements-build.txt  # 构建依赖
├── build.py                # 一键打包脚本
└── README.md
```

## License

仅供学习交流使用。
